# 이 파일을 .env.local 로 복사한 뒤 발급받은 키를 채워 넣으세요.
#   cp .env.example .env.local
#
# .env.local 은 .gitignore 의 *.local 규칙에 걸려 커밋되지 않습니다.
# Vite 는 VITE_ 로 시작하는 변수만 앱 코드에 노출합니다.
#
# 키 발급: https://home.openweathermap.org/api_keys
# (가입 직후에는 키가 활성화되기까지 10분~2시간 정도 걸릴 수 있습니다)
VITE_OPENWEATHER_API_KEY=
